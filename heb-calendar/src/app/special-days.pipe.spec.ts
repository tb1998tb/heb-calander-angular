import { SpecialDaysPipe } from './special-days.pipe';

describe('SpecialDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
